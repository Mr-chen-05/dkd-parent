## 原因
- 角色的 `el-form-item` 没有设置 `prop`，校验信息无法挂载在该表单项上（`index.vue:211`）。
- 规则里为 `roleId` 设置的是 `trigger: "blur"`，而 `el-select` 组件只在 `change` 触发校验（`index.vue:286`）。

## 修改方案
- 在角色表单项上添加 `prop="roleId"`：`e:\ideaProgram\dkd\dkd-vue\src\views\manage\personnel\emp\index.vue:211`。
- 将规则 `rules.roleId` 的触发方式改为 `change`：`index.vue:286`。

## 具体改动
- `index.vue:211`：`<el-form-item label="角色">` → `<el-form-item label="角色" prop="roleId">`
- `index.vue:286`：`trigger: "blur"` → `trigger: "change"`

## 验证步骤
- 点击“新增”，保持角色为空，点“确定”，应在角色下方出现“角色不能为空”。
- 选择任意角色后，错误提示消失；再次提交应通过该项校验。

请确认后我将直接修改并进行本地验证。