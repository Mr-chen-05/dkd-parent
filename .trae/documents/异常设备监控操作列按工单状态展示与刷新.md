## 目标
- 操作列按工单状态展示：1“工单已创建”、2“工单进行中”、3“已取消”；4“已完成”的设备不再显示在异常设备监控列表。
- 创建成功后立即显示“已创建”，随后刷新接口与后端状态对齐。

## 改动文件
- e:\ideaProgram\dkd\dkd-vue\src\views\home\components\abnormal-equipment-table.vue

## 代码改动
- 更新操作列模板：用 `taskStatus` 判断展示文案，未定义/0 显示“创建工单”按钮。
- 在 `reloadList()` 中对接口返回数据执行前端过滤：剔除 `taskStatus === 4` 的记录，确保已完成不出现在列表。
- 在单个与批量创建成功分支：设置 `vm.taskStatus = 1` 并调用 `await reloadList()`；保留 `hasActiveTask = true` 以兼容旧逻辑。
- 增加状态样式类以提升辨识度。

## 验证
- 创建前显示按钮；创建成功后显示“已创建”，刷新后若流转到“进行中/取消”正确显示；“已完成”在后续刷新后从列表消失。

## 兼容
- 无后端改动，低风险；如后端已过滤完成项，前端过滤为冗余但不影响表现。